<!DOCTYPE html>
<html>
<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-alpha2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel='stylesheet' href='/stylesheets/style.css'/>

</head>
<body>
<div class="container mt-5" id="tabela">
    <table class="table table-striped">
        <thead class="bg-dark text-white">
        <tr>
            <th>ID taska</th>
            <th>Naziv</th>
            <th>Opis</th>
        </tr>
        </thead>
        <tbody id="tabelaTaskova">
        <% for (let i = 0; i < taskovi.length; i++) { %>
            <tr id="red<%= taskovi[i].id_taska %>">
                <td><%= taskovi[i].id_taska %></td>
                <td id="naziv<%= taskovi[i].id_taska %>"><%= taskovi[i].naziv %></td>
                <td id="opis<%= taskovi[i].id_taska %>"><%= taskovi[i].opis %></td>
            </tr>
        <% } %>

        </tbody>
    </table>
</div>


</body>
</html>
